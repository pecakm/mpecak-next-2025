'use client';

import { Container, Button } from './header.styled';

export default function Header() {
  return (
    <Container>
      Header
      <Button>
        Test
      </Button>
    </Container>
  );
}
